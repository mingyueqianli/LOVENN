# Security Policy

Do not post real node links, UUID, passwords, tokens, private keys, certificates, or full config files publicly.

Before opening an issue, run:

```bash
Love support-bundle
```

Review the generated bundle and remove any remaining sensitive values.
